2. Object 클래스에 대한 설명 중 틀린 것은 무엇입니까? 2
➊ 모든 자바 클래스의 최상위 부모 클래스이다.
➋ Object의 equals ( ) 메소드는 == 연산자와 동일하게 번지를 비교한다.
➌ Object의 hashCode( ) 메소드는 동등 비교 시 활용된다.
➍ Object의 toString ( ) 메소드는 객체의 필드값을 문자열로 리턴한다.
